name: Update veille page

on:
  workflow_dispatch:
  schedule:
    - cron: "0 7 * * 3"

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install openpyxl requests

      - name: Fetch BOAMP data
        run: python scripts/fetch_boamp.py

      - name: Run generator
        run: python scripts/generate.py

      - name: Commit generated files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/veille.json index.html veille-marches-caps.xlsx
          git diff --staged --quiet || git commit -m "Mise à jour automatique de la veille"
          git push
